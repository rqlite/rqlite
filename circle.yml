dependencies:
    post:
        - ./gen_artifacts.sh

test:
    override:
        - bash gofmt.sh
        - go tool vet .
        - go test -timeout 60s -v ./...
        - GORACE="halt_on_error=1" go test -race -timeout 120s -v ./...
